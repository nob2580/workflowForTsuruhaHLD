package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Tsuuchi;

/**
 * 通知に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class TsuuchiAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Tsuuchi mapToDto(GMap map){
		return map == null ? null : new Tsuuchi(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Tsuuchi> mapToDto(List<GMap> mapList){
		List<Tsuuchi> dtoList = new ArrayList<Tsuuchi>();
		for (var map : mapList) {
			dtoList.add(new Tsuuchi(map));
		}
		return dtoList;
	}
	
	/**
	 * 通知のレコード有無を判定
	 * @param serialNo シリアル番号
	 * @return true:exist false:not exist
	 */
	public boolean exists(long serialNo) {
		return this.find(serialNo) == null ? false : true;
	}
	
	/**
	 * 通知から主キー指定でレコードを取得
	 * @param serialNo シリアル番号
	 * @return 通知DTO
	 */
	public Tsuuchi find(long serialNo) {
		final String sql = "SELECT * FROM tsuuchi WHERE serial_no = ?";
		return mapToDto(connection.find(sql, serialNo));
	}
	
	/**
	 * 通知からレコードを全件取得 ※大量データ取得に注意
	 * @return List<通知DTO>
	 */
	public List<Tsuuchi> load() {
		final String sql = "SELECT * FROM tsuuchi ORDER BY serial_no";
		return mapToDto(connection.load(sql));
	}

	/**
	* 通知登録
	* @param dto 通知
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int insert(
		Tsuuchi dto
		,String koushinUserId
	){
		final String sql =
				"INSERT INTO tsuuchi "
			+ "( user_id, denpyou_id, edano, kidoku_flg, touroku_user_id, touroku_time, koushin_user_id, koushin_time) "
			+ "VALUES(?, ?, ?, ?, ?, current_timestamp, ?, current_timestamp "
			+ ")";
			return connection.update(sql,
					dto.userId, dto.denpyouId, dto.edano, dto.kidokuFlg, koushinUserId, koushinUserId
					);
	}

	/**
	* 通知の非キー全てと共通列を更新
	* 値設定漏れによるデータ消失防止のためDaoで取得したTsuuchiの使用を前提
	* @param dto 通知
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int update(
		Tsuuchi dto
		,String koushinUserId
		 ){
		final String sql =
				"UPDATE tsuuchi "
		    + "SET user_id = ?, denpyou_id = ?, edano = ?, kidoku_flg = ?, koushin_user_id = ?, koushin_time = current_timestamp "
	 		+ "WHERE koushin_time = ? AND serial_no = ?";
			return connection.update(sql,
				dto.userId, dto.denpyouId, dto.edano, dto.kidokuFlg, koushinUserId
				,dto.koushinTime, dto.serialNo);
    }

	/**
	* 通知登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto 通知
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int upsert(
		Tsuuchi dto
		,String koushinUserId
		 ){
		final String sql =
				"INSERT INTO tsuuchi "
			+ "( user_id, denpyou_id, edano, kidoku_flg, touroku_user_id, touroku_time, koushin_user_id, koushin_time) "
			+ "VALUES(?, ?, ?, ?, ?, current_timestamp, ?, current_timestamp "
			+ ") ON CONFLICT ON CONSTRAINT tsuuchi_pkey "
			+ "DO UPDATE SET user_id = ?, denpyou_id = ?, edano = ?, kidoku_flg = ?, koushin_user_id = ?, koushin_time = current_timestamp "
			+ "";
			return connection.update(sql,
				dto.userId, dto.denpyouId, dto.edano, dto.kidokuFlg, koushinUserId, koushinUserId
				, dto.userId, dto.denpyouId, dto.edano, dto.kidokuFlg, koushinUserId
				);
    }
	
	/**
	 * 通知から主キー指定でレコードを削除
	 * @param serialNo シリアル番号
	 * @return 削除件数
	 */
	public int delete(long serialNo){
		final String sql = "DELETE FROM tsuuchi WHERE serial_no = ?";
		return connection.update(sql, serialNo);
	}
}
