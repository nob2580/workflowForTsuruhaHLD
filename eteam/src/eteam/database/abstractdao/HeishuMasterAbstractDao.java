package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.HeishuMaster;

/**
 * 幣種マスターに対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class HeishuMasterAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected HeishuMaster mapToDto(GMap map){
		return map == null ? null : new HeishuMaster(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<HeishuMaster> mapToDto(List<GMap> mapList){
		List<HeishuMaster> dtoList = new ArrayList<HeishuMaster>();
		for (var map : mapList) {
			dtoList.add(new HeishuMaster(map));
		}
		return dtoList;
	}
	
	/**
	 * 幣種マスターのレコード有無を判定
	 * @param heishuCd 幣種コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String heishuCd) {
		return this.find(heishuCd) == null ? false : true;
	}
	
	/**
	 * 幣種マスターから主キー指定でレコードを取得
	 * @param heishuCd 幣種コード
	 * @return 幣種マスターDTO
	 */
	public HeishuMaster find(String heishuCd) {
		final String sql = "SELECT * FROM heishu_master WHERE heishu_cd = ?";
		return mapToDto(connection.find(sql, heishuCd));
	}
	
	/**
	 * 幣種マスターからレコードを全件取得 ※大量データ取得に注意
	 * @return List<幣種マスターDTO>
	 */
	public List<HeishuMaster> load() {
		final String sql = "SELECT * FROM heishu_master ORDER BY heishu_cd";
		return mapToDto(connection.load(sql));
	}

	/**
	* 幣種マスター登録
	* @param dto 幣種マスター
	* @return 件数
	*/
	public int insert(
		HeishuMaster dto
	){
		final String sql =
				"INSERT INTO heishu_master "
			+ "VALUES(?, ?, ?, ?, ?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.heishuCd, dto.currencyUnit, dto.countryName, dto.conversionUnit, dto.decimalPosition, dto.availabilityFlg, dto.displayOrder
					);
	}

	/**
	* 幣種マスター登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto 幣種マスター
	* @return 件数
	*/
	public int upsert(
		HeishuMaster dto
		 ){
		final String sql =
				"INSERT INTO heishu_master "
			+ "VALUES(?, ?, ?, ?, ?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT heishu_master_pkey "
			+ "DO UPDATE SET currency_unit = ?, country_name = ?, conversion_unit = ?, decimal_position = ?, availability_flg = ?, display_order = ? "
			+ "";
			return connection.update(sql,
				dto.heishuCd, dto.currencyUnit, dto.countryName, dto.conversionUnit, dto.decimalPosition, dto.availabilityFlg, dto.displayOrder
				, dto.currencyUnit, dto.countryName, dto.conversionUnit, dto.decimalPosition, dto.availabilityFlg, dto.displayOrder
				);
    }
	
	/**
	 * 幣種マスターから主キー指定でレコードを削除
	 * @param heishuCd 幣種コード
	 * @return 削除件数
	 */
	public int delete(String heishuCd){
		final String sql = "DELETE FROM heishu_master WHERE heishu_cd = ?";
		return connection.update(sql, heishuCd);
	}
}
