package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Uf4Ichiran;

/**
 * ユニバーサルフィールド４一覧に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Uf4IchiranAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Uf4Ichiran mapToDto(GMap map){
		return map == null ? null : new Uf4Ichiran(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Uf4Ichiran> mapToDto(List<GMap> mapList){
		List<Uf4Ichiran> dtoList = new ArrayList<Uf4Ichiran>();
		for (var map : mapList) {
			dtoList.add(new Uf4Ichiran(map));
		}
		return dtoList;
	}
	
	/**
	 * ユニバーサルフィールド４一覧のレコード有無を判定
	 * @param uf4Cd UF4コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String uf4Cd) {
		return this.find(uf4Cd) == null ? false : true;
	}
	
	/**
	 * ユニバーサルフィールド４一覧から主キー指定でレコードを取得
	 * @param uf4Cd UF4コード
	 * @return ユニバーサルフィールド４一覧DTO
	 */
	public Uf4Ichiran find(String uf4Cd) {
		final String sql = "SELECT * FROM uf4_ichiran WHERE uf4_cd = ?";
		return mapToDto(connection.find(sql, uf4Cd));
	}
	
	/**
	 * ユニバーサルフィールド４一覧からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ユニバーサルフィールド４一覧DTO>
	 */
	public List<Uf4Ichiran> load() {
		final String sql = "SELECT * FROM uf4_ichiran ORDER BY uf4_cd";
		return mapToDto(connection.load(sql));
	}

	/**
	* ユニバーサルフィールド４一覧登録
	* @param dto ユニバーサルフィールド４一覧
	* @return 件数
	*/
	public int insert(
		Uf4Ichiran dto
	){
		final String sql =
				"INSERT INTO uf4_ichiran "
			+ "VALUES(?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.uf4Cd, dto.uf4NameRyakushiki, dto.kessankiBangou
					);
	}

	/**
	* ユニバーサルフィールド４一覧登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ユニバーサルフィールド４一覧
	* @return 件数
	*/
	public int upsert(
		Uf4Ichiran dto
		 ){
		final String sql =
				"INSERT INTO uf4_ichiran "
			+ "VALUES(?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT uf4_ichiran_pkey "
			+ "DO UPDATE SET uf4_name_ryakushiki = ?, kessanki_bangou = ? "
			+ "";
			return connection.update(sql,
				dto.uf4Cd, dto.uf4NameRyakushiki, dto.kessankiBangou
				, dto.uf4NameRyakushiki, dto.kessankiBangou
				);
    }
	
	/**
	 * ユニバーサルフィールド４一覧から主キー指定でレコードを削除
	 * @param uf4Cd UF4コード
	 * @return 削除件数
	 */
	public int delete(String uf4Cd){
		final String sql = "DELETE FROM uf4_ichiran WHERE uf4_cd = ?";
		return connection.update(sql, uf4Cd);
	}
}
