package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.KaniTodoke;

/**
 * 届出ジェネレータに対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class KaniTodokeAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected KaniTodoke mapToDto(GMap map){
		return map == null ? null : new KaniTodoke(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<KaniTodoke> mapToDto(List<GMap> mapList){
		List<KaniTodoke> dtoList = new ArrayList<KaniTodoke>();
		for (var map : mapList) {
			dtoList.add(new KaniTodoke(map));
		}
		return dtoList;
	}
	
	/**
	 * 届出ジェネレータのレコード有無を判定
	 * @param denpyouId 伝票ID
	 * @param itemName 項目名
	 * @return true:exist false:not exist
	 */
	public boolean exists(String denpyouId, String itemName) {
		return this.find(denpyouId, itemName) == null ? false : true;
	}
	
	/**
	 * 届出ジェネレータから主キー指定でレコードを取得
	 * @param denpyouId 伝票ID
	 * @param itemName 項目名
	 * @return 届出ジェネレータDTO
	 */
	public KaniTodoke find(String denpyouId, String itemName) {
		final String sql = "SELECT * FROM kani_todoke WHERE denpyou_id = ? AND item_name = ?";
		return mapToDto(connection.find(sql, denpyouId, itemName));
	}
	
	/**
	 * 届出ジェネレータからレコードを全件取得 ※大量データ取得に注意
	 * @return List<届出ジェネレータDTO>
	 */
	public List<KaniTodoke> load() {
		final String sql = "SELECT * FROM kani_todoke ORDER BY denpyou_id, item_name";
		return mapToDto(connection.load(sql));
	}
	
	/**
	 * 届出ジェネレータから一部キー指定でレコードを取得
	 * @param denpyouId 伝票ID
	 * @return List<届出ジェネレータ>DTO
	 */
	public List<KaniTodoke> load(String denpyouId) {
		final String sql = "SELECT * FROM kani_todoke WHERE denpyou_id = ? "
							+ "ORDER BY denpyou_id, item_name";
		return mapToDto(connection.load(sql, denpyouId));
	}

	/**
	* 届出ジェネレータ登録
	* @param dto 届出ジェネレータ
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int insert(
		KaniTodoke dto
		,String koushinUserId
	){
		final String sql =
				"INSERT INTO kani_todoke "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, current_timestamp, ?, current_timestamp "
			+ ")";
			return connection.update(sql,
					dto.denpyouId, dto.denpyouKbn, dto.version, dto.itemName, dto.value1, dto.value2, koushinUserId, koushinUserId
					);
	}

	/**
	* 届出ジェネレータの非キー全てと共通列を更新
	* 値設定漏れによるデータ消失防止のためDaoで取得したKaniTodokeの使用を前提
	* @param dto 届出ジェネレータ
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int update(
		KaniTodoke dto
		,String koushinUserId
		 ){
		final String sql =
				"UPDATE kani_todoke "
		    + "SET denpyou_kbn = ?, version = ?, value1 = ?, value2 = ?, koushin_user_id = ?, koushin_time = current_timestamp "
	 		+ "WHERE koushin_time = ? AND denpyou_id = ? AND item_name = ?";
			return connection.update(sql,
				dto.denpyouKbn, dto.version, dto.value1, dto.value2, koushinUserId
				,dto.koushinTime, dto.denpyouId, dto.itemName);
    }

	/**
	* 届出ジェネレータ登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto 届出ジェネレータ
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int upsert(
		KaniTodoke dto
		,String koushinUserId
		 ){
		final String sql =
				"INSERT INTO kani_todoke "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, current_timestamp, ?, current_timestamp "
			+ ") ON CONFLICT ON CONSTRAINT kani_todoke_pkey "
			+ "DO UPDATE SET denpyou_kbn = ?, version = ?, value1 = ?, value2 = ?, koushin_user_id = ?, koushin_time = current_timestamp "
			+ "";
			return connection.update(sql,
				dto.denpyouId, dto.denpyouKbn, dto.version, dto.itemName, dto.value1, dto.value2, koushinUserId, koushinUserId
				, dto.denpyouKbn, dto.version, dto.value1, dto.value2, koushinUserId
				);
    }
	
	/**
	 * 届出ジェネレータから主キー指定でレコードを削除
	 * @param denpyouId 伝票ID
	 * @param itemName 項目名
	 * @return 削除件数
	 */
	public int delete(String denpyouId, String itemName){
		final String sql = "DELETE FROM kani_todoke WHERE denpyou_id = ? AND item_name = ?";
		return connection.update(sql, denpyouId, itemName);
	}
	
	/**
	 * 届出ジェネレータから一部キー指定でレコードを削除
	 * @param denpyouId 伝票ID
	 * @return 削除件数
	 */
	public int delete(String denpyouId) {
		final String sql = "DELETE FROM kani_todoke WHERE denpyou_id = ? ";
		return connection.update(sql, denpyouId);
	}
}
