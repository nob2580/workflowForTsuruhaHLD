package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.KoutsuuShudanMaster;

/**
 * 国内用交通手段マスターに対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class KoutsuuShudanMasterAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected KoutsuuShudanMaster mapToDto(GMap map){
		return map == null ? null : new KoutsuuShudanMaster(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<KoutsuuShudanMaster> mapToDto(List<GMap> mapList){
		List<KoutsuuShudanMaster> dtoList = new ArrayList<KoutsuuShudanMaster>();
		for (var map : mapList) {
			dtoList.add(new KoutsuuShudanMaster(map));
		}
		return dtoList;
	}
	
	/**
	 * 国内用交通手段マスターのレコード有無を判定
	 * @param sortJun 並び順
	 * @param koutsuuShudan 交通手段
	 * @return true:exist false:not exist
	 */
	public boolean exists(String sortJun, String koutsuuShudan) {
		return this.find(sortJun, koutsuuShudan) == null ? false : true;
	}
	
	/**
	 * 国内用交通手段マスターから主キー指定でレコードを取得
	 * @param sortJun 並び順
	 * @param koutsuuShudan 交通手段
	 * @return 国内用交通手段マスターDTO
	 */
	public KoutsuuShudanMaster find(String sortJun, String koutsuuShudan) {
		final String sql = "SELECT * FROM koutsuu_shudan_master WHERE sort_jun = ? AND koutsuu_shudan = ?";
		return mapToDto(connection.find(sql, sortJun, koutsuuShudan));
	}
	
	/**
	 * 国内用交通手段マスターからレコードを全件取得 ※大量データ取得に注意
	 * @return List<国内用交通手段マスターDTO>
	 */
	public List<KoutsuuShudanMaster> load() {
		final String sql = "SELECT * FROM koutsuu_shudan_master ORDER BY sort_jun, koutsuu_shudan";
		return mapToDto(connection.load(sql));
	}
	
	/**
	 * 国内用交通手段マスターから一部キー指定でレコードを取得
	 * @param sortJun 並び順
	 * @return List<国内用交通手段マスター>DTO
	 */
	public List<KoutsuuShudanMaster> load(String sortJun) {
		final String sql = "SELECT * FROM koutsuu_shudan_master WHERE sort_jun = ? "
							+ "ORDER BY sort_jun, koutsuu_shudan";
		return mapToDto(connection.load(sql, sortJun));
	}

	/**
	* 国内用交通手段マスター登録
	* @param dto 国内用交通手段マスター
	* @return 件数
	*/
	public int insert(
		KoutsuuShudanMaster dto
	){
		final String sql =
				"INSERT INTO koutsuu_shudan_master "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.sortJun, dto.koutsuuShudan, dto.shouhyouShoruiHissuFlg, dto.zeiKubun, dto.edaban, dto.suuryouNyuryokuType, dto.tanka, dto.suuryouKigou
					);
	}

	/**
	* 国内用交通手段マスター登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto 国内用交通手段マスター
	* @return 件数
	*/
	public int upsert(
		KoutsuuShudanMaster dto
		 ){
		final String sql =
				"INSERT INTO koutsuu_shudan_master "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT koutsuu_shudan_master_pkey "
			+ "DO UPDATE SET shouhyou_shorui_hissu_flg = ?, zei_kubun = ?, edaban = ?, suuryou_nyuryoku_type = ?, tanka = ?, suuryou_kigou = ? "
			+ "";
			return connection.update(sql,
				dto.sortJun, dto.koutsuuShudan, dto.shouhyouShoruiHissuFlg, dto.zeiKubun, dto.edaban, dto.suuryouNyuryokuType, dto.tanka, dto.suuryouKigou
				, dto.shouhyouShoruiHissuFlg, dto.zeiKubun, dto.edaban, dto.suuryouNyuryokuType, dto.tanka, dto.suuryouKigou
				);
    }
	
	/**
	 * 国内用交通手段マスターから主キー指定でレコードを削除
	 * @param sortJun 並び順
	 * @param koutsuuShudan 交通手段
	 * @return 削除件数
	 */
	public int delete(String sortJun, String koutsuuShudan){
		final String sql = "DELETE FROM koutsuu_shudan_master WHERE sort_jun = ? AND koutsuu_shudan = ?";
		return connection.update(sql, sortJun, koutsuuShudan);
	}
	
	/**
	 * 国内用交通手段マスターから一部キー指定でレコードを削除
	 * @param sortJun 並び順
	 * @return 削除件数
	 */
	public int delete(String sortJun) {
		final String sql = "DELETE FROM koutsuu_shudan_master WHERE sort_jun = ? ";
		return connection.update(sql, sortJun);
	}
}
