package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.TorihikisakiKamokuZandaka;

/**
 * 取引先科目残高に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class TorihikisakiKamokuZandakaAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected TorihikisakiKamokuZandaka mapToDto(GMap map){
		return map == null ? null : new TorihikisakiKamokuZandaka(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<TorihikisakiKamokuZandaka> mapToDto(List<GMap> mapList){
		List<TorihikisakiKamokuZandaka> dtoList = new ArrayList<TorihikisakiKamokuZandaka>();
		for (var map : mapList) {
			dtoList.add(new TorihikisakiKamokuZandaka(map));
		}
		return dtoList;
	}
	
	/**
	 * 取引先科目残高のレコード有無を判定
	 * @param torihikisakiCd 取引先コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String torihikisakiCd, String kamokuGaibuCd) {
		return this.find(torihikisakiCd, kamokuGaibuCd) == null ? false : true;
	}
	
	/**
	 * 取引先科目残高から主キー指定でレコードを取得
	 * @param torihikisakiCd 取引先コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return 取引先科目残高DTO
	 */
	public TorihikisakiKamokuZandaka find(String torihikisakiCd, String kamokuGaibuCd) {
		final String sql = "SELECT * FROM torihikisaki_kamoku_zandaka WHERE torihikisaki_cd = ? AND kamoku_gaibu_cd = ?";
		return mapToDto(connection.find(sql, torihikisakiCd, kamokuGaibuCd));
	}
	
	/**
	 * 取引先科目残高からレコードを全件取得 ※大量データ取得に注意
	 * @return List<取引先科目残高DTO>
	 */
	public List<TorihikisakiKamokuZandaka> load() {
		final String sql = "SELECT * FROM torihikisaki_kamoku_zandaka ORDER BY torihikisaki_cd, kamoku_gaibu_cd";
		return mapToDto(connection.load(sql));
	}
	
	/**
	 * 取引先科目残高から一部キー指定でレコードを取得
	 * @param torihikisakiCd 取引先コード
	 * @return List<取引先科目残高>DTO
	 */
	public List<TorihikisakiKamokuZandaka> load(String torihikisakiCd) {
		final String sql = "SELECT * FROM torihikisaki_kamoku_zandaka WHERE torihikisaki_cd = ? "
							+ "ORDER BY torihikisaki_cd, kamoku_gaibu_cd";
		return mapToDto(connection.load(sql, torihikisakiCd));
	}

	/**
	* 取引先科目残高登録
	* @param dto 取引先科目残高
	* @return 件数
	*/
	public int insert(
		TorihikisakiKamokuZandaka dto
	){
		final String sql =
				"INSERT INTO torihikisaki_kamoku_zandaka "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.torihikisakiCd, dto.kamokuGaibuCd, dto.kessankiBangou, dto.kamokuNaibuCd, dto.torihikisakiNameRyakushiki, dto.torihikisakiNameSeishiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
					);
	}

	/**
	* 取引先科目残高登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto 取引先科目残高
	* @return 件数
	*/
	public int upsert(
		TorihikisakiKamokuZandaka dto
		 ){
		final String sql =
				"INSERT INTO torihikisaki_kamoku_zandaka "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT torihikisaki_kamoku_zandaka_pkey "
			+ "DO UPDATE SET kessanki_bangou = ?, kamoku_naibu_cd = ?, torihikisaki_name_ryakushiki = ?, torihikisaki_name_seishiki = ?, chouhyou_shaturyoku_no = ?, kamoku_name_ryakushiki = ?, kamoku_name_seishiki = ?, taishaku_zokusei = ?, kishu_zandaka = ? "
			+ "";
			return connection.update(sql,
				dto.torihikisakiCd, dto.kamokuGaibuCd, dto.kessankiBangou, dto.kamokuNaibuCd, dto.torihikisakiNameRyakushiki, dto.torihikisakiNameSeishiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
				, dto.kessankiBangou, dto.kamokuNaibuCd, dto.torihikisakiNameRyakushiki, dto.torihikisakiNameSeishiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
				);
    }
	
	/**
	 * 取引先科目残高から主キー指定でレコードを削除
	 * @param torihikisakiCd 取引先コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return 削除件数
	 */
	public int delete(String torihikisakiCd, String kamokuGaibuCd){
		final String sql = "DELETE FROM torihikisaki_kamoku_zandaka WHERE torihikisaki_cd = ? AND kamoku_gaibu_cd = ?";
		return connection.update(sql, torihikisakiCd, kamokuGaibuCd);
	}
	
	/**
	 * 取引先科目残高から一部キー指定でレコードを削除
	 * @param torihikisakiCd 取引先コード
	 * @return 削除件数
	 */
	public int delete(String torihikisakiCd) {
		final String sql = "DELETE FROM torihikisaki_kamoku_zandaka WHERE torihikisaki_cd = ? ";
		return connection.update(sql, torihikisakiCd);
	}
}
