package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Uf4Zandaka;

/**
 * ユニバーサルフィールド４残高に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Uf4ZandakaAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Uf4Zandaka mapToDto(GMap map){
		return map == null ? null : new Uf4Zandaka(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Uf4Zandaka> mapToDto(List<GMap> mapList){
		List<Uf4Zandaka> dtoList = new ArrayList<Uf4Zandaka>();
		for (var map : mapList) {
			dtoList.add(new Uf4Zandaka(map));
		}
		return dtoList;
	}
	
	/**
	 * ユニバーサルフィールド４残高のレコード有無を判定
	 * @param uf4Cd UF4コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String uf4Cd, String kamokuGaibuCd) {
		return this.find(uf4Cd, kamokuGaibuCd) == null ? false : true;
	}
	
	/**
	 * ユニバーサルフィールド４残高から主キー指定でレコードを取得
	 * @param uf4Cd UF4コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return ユニバーサルフィールド４残高DTO
	 */
	public Uf4Zandaka find(String uf4Cd, String kamokuGaibuCd) {
		final String sql = "SELECT * FROM uf4_zandaka WHERE uf4_cd = ? AND kamoku_gaibu_cd = ?";
		return mapToDto(connection.find(sql, uf4Cd, kamokuGaibuCd));
	}
	
	/**
	 * ユニバーサルフィールド４残高からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ユニバーサルフィールド４残高DTO>
	 */
	public List<Uf4Zandaka> load() {
		final String sql = "SELECT * FROM uf4_zandaka ORDER BY uf4_cd, kamoku_gaibu_cd";
		return mapToDto(connection.load(sql));
	}
	
	/**
	 * ユニバーサルフィールド４残高から一部キー指定でレコードを取得
	 * @param uf4Cd UF4コード
	 * @return List<ユニバーサルフィールド４残高>DTO
	 */
	public List<Uf4Zandaka> load(String uf4Cd) {
		final String sql = "SELECT * FROM uf4_zandaka WHERE uf4_cd = ? "
							+ "ORDER BY uf4_cd, kamoku_gaibu_cd";
		return mapToDto(connection.load(sql, uf4Cd));
	}

	/**
	* ユニバーサルフィールド４残高登録
	* @param dto ユニバーサルフィールド４残高
	* @return 件数
	*/
	public int insert(
		Uf4Zandaka dto
	){
		final String sql =
				"INSERT INTO uf4_zandaka "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.uf4Cd, dto.kamokuGaibuCd, dto.kessankiBangou, dto.kamokuNaibuCd, dto.uf4NameRyakushiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
					);
	}

	/**
	* ユニバーサルフィールド４残高登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ユニバーサルフィールド４残高
	* @return 件数
	*/
	public int upsert(
		Uf4Zandaka dto
		 ){
		final String sql =
				"INSERT INTO uf4_zandaka "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT uf4_zandaka_pkey "
			+ "DO UPDATE SET kessanki_bangou = ?, kamoku_naibu_cd = ?, uf4_name_ryakushiki = ?, chouhyou_shaturyoku_no = ?, kamoku_name_ryakushiki = ?, kamoku_name_seishiki = ?, taishaku_zokusei = ?, kishu_zandaka = ? "
			+ "";
			return connection.update(sql,
				dto.uf4Cd, dto.kamokuGaibuCd, dto.kessankiBangou, dto.kamokuNaibuCd, dto.uf4NameRyakushiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
				, dto.kessankiBangou, dto.kamokuNaibuCd, dto.uf4NameRyakushiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
				);
    }
	
	/**
	 * ユニバーサルフィールド４残高から主キー指定でレコードを削除
	 * @param uf4Cd UF4コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return 削除件数
	 */
	public int delete(String uf4Cd, String kamokuGaibuCd){
		final String sql = "DELETE FROM uf4_zandaka WHERE uf4_cd = ? AND kamoku_gaibu_cd = ?";
		return connection.update(sql, uf4Cd, kamokuGaibuCd);
	}
	
	/**
	 * ユニバーサルフィールド４残高から一部キー指定でレコードを削除
	 * @param uf4Cd UF4コード
	 * @return 削除件数
	 */
	public int delete(String uf4Cd) {
		final String sql = "DELETE FROM uf4_zandaka WHERE uf4_cd = ? ";
		return connection.update(sql, uf4Cd);
	}
}
