package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.SegmentKamokuZandaka;

/**
 * セグメント科目残高に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class SegmentKamokuZandakaAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected SegmentKamokuZandaka mapToDto(GMap map){
		return map == null ? null : new SegmentKamokuZandaka(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<SegmentKamokuZandaka> mapToDto(List<GMap> mapList){
		List<SegmentKamokuZandaka> dtoList = new ArrayList<SegmentKamokuZandaka>();
		for (var map : mapList) {
			dtoList.add(new SegmentKamokuZandaka(map));
		}
		return dtoList;
	}
	
	/**
	 * セグメント科目残高のレコード有無を判定
	 * @param segmentCd セグメントコード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String segmentCd, String kamokuGaibuCd) {
		return this.find(segmentCd, kamokuGaibuCd) == null ? false : true;
	}
	
	/**
	 * セグメント科目残高から主キー指定でレコードを取得
	 * @param segmentCd セグメントコード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return セグメント科目残高DTO
	 */
	public SegmentKamokuZandaka find(String segmentCd, String kamokuGaibuCd) {
		final String sql = "SELECT * FROM segment_kamoku_zandaka WHERE segment_cd = ? AND kamoku_gaibu_cd = ?";
		return mapToDto(connection.find(sql, segmentCd, kamokuGaibuCd));
	}
	
	/**
	 * セグメント科目残高からレコードを全件取得 ※大量データ取得に注意
	 * @return List<セグメント科目残高DTO>
	 */
	public List<SegmentKamokuZandaka> load() {
		final String sql = "SELECT * FROM segment_kamoku_zandaka ORDER BY segment_cd, kamoku_gaibu_cd";
		return mapToDto(connection.load(sql));
	}
	
	/**
	 * セグメント科目残高から一部キー指定でレコードを取得
	 * @param segmentCd セグメントコード
	 * @return List<セグメント科目残高>DTO
	 */
	public List<SegmentKamokuZandaka> load(String segmentCd) {
		final String sql = "SELECT * FROM segment_kamoku_zandaka WHERE segment_cd = ? "
							+ "ORDER BY segment_cd, kamoku_gaibu_cd";
		return mapToDto(connection.load(sql, segmentCd));
	}

	/**
	* セグメント科目残高登録
	* @param dto セグメント科目残高
	* @return 件数
	*/
	public int insert(
		SegmentKamokuZandaka dto
	){
		final String sql =
				"INSERT INTO segment_kamoku_zandaka "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.segmentCd, dto.kamokuGaibuCd, dto.kessankiBangou, dto.kamokuNaibuCd, dto.torihikisakiNameRyakushiki, dto.torihikisakiNameSeishiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
					);
	}

	/**
	* セグメント科目残高登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto セグメント科目残高
	* @return 件数
	*/
	public int upsert(
		SegmentKamokuZandaka dto
		 ){
		final String sql =
				"INSERT INTO segment_kamoku_zandaka "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT segment_kamoku_zandaka_pkey "
			+ "DO UPDATE SET kessanki_bangou = ?, kamoku_naibu_cd = ?, torihikisaki_name_ryakushiki = ?, torihikisaki_name_seishiki = ?, chouhyou_shaturyoku_no = ?, kamoku_name_ryakushiki = ?, kamoku_name_seishiki = ?, taishaku_zokusei = ?, kishu_zandaka = ? "
			+ "";
			return connection.update(sql,
				dto.segmentCd, dto.kamokuGaibuCd, dto.kessankiBangou, dto.kamokuNaibuCd, dto.torihikisakiNameRyakushiki, dto.torihikisakiNameSeishiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
				, dto.kessankiBangou, dto.kamokuNaibuCd, dto.torihikisakiNameRyakushiki, dto.torihikisakiNameSeishiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
				);
    }
	
	/**
	 * セグメント科目残高から主キー指定でレコードを削除
	 * @param segmentCd セグメントコード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return 削除件数
	 */
	public int delete(String segmentCd, String kamokuGaibuCd){
		final String sql = "DELETE FROM segment_kamoku_zandaka WHERE segment_cd = ? AND kamoku_gaibu_cd = ?";
		return connection.update(sql, segmentCd, kamokuGaibuCd);
	}
	
	/**
	 * セグメント科目残高から一部キー指定でレコードを削除
	 * @param segmentCd セグメントコード
	 * @return 削除件数
	 */
	public int delete(String segmentCd) {
		final String sql = "DELETE FROM segment_kamoku_zandaka WHERE segment_cd = ? ";
		return connection.update(sql, segmentCd);
	}
}
