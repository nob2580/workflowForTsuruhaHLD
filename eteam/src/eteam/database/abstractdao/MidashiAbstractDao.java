package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Midashi;

/**
 * 見出しに対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class MidashiAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Midashi mapToDto(GMap map){
		return map == null ? null : new Midashi(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Midashi> mapToDto(List<GMap> mapList){
		List<Midashi> dtoList = new ArrayList<Midashi>();
		for (var map : mapList) {
			dtoList.add(new Midashi(map));
		}
		return dtoList;
	}
	
	/**
	 * 見出しのレコード有無を判定
	 * @param midashiId 見出しID
	 * @return true:exist false:not exist
	 */
	public boolean exists(long midashiId) {
		return this.find(midashiId) == null ? false : true;
	}
	
	/**
	 * 見出しから主キー指定でレコードを取得
	 * @param midashiId 見出しID
	 * @return 見出しDTO
	 */
	public Midashi find(long midashiId) {
		final String sql = "SELECT * FROM midashi WHERE midashi_id = ?";
		return mapToDto(connection.find(sql, midashiId));
	}
	
	/**
	 * 見出しからレコードを全件取得 ※大量データ取得に注意
	 * @return List<見出しDTO>
	 */
	public List<Midashi> load() {
		final String sql = "SELECT * FROM midashi ORDER BY midashi_id";
		return mapToDto(connection.load(sql));
	}

	/**
	* 見出し登録
	* @param dto 見出し
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int insert(
		Midashi dto
		,String koushinUserId
	){
		final String sql =
				"INSERT INTO midashi "
			+ "( midashi_name, hyouji_jun, touroku_user_id, touroku_time, koushin_user_id, koushin_time) "
			+ "VALUES(?, ?, ?, current_timestamp, ?, current_timestamp "
			+ ")";
			return connection.update(sql,
					dto.midashiName, dto.hyoujiJun, koushinUserId, koushinUserId
					);
	}

	/**
	* 見出しの非キー全てと共通列を更新
	* 値設定漏れによるデータ消失防止のためDaoで取得したMidashiの使用を前提
	* @param dto 見出し
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int update(
		Midashi dto
		,String koushinUserId
		 ){
		final String sql =
				"UPDATE midashi "
		    + "SET midashi_name = ?, hyouji_jun = ?, koushin_user_id = ?, koushin_time = current_timestamp "
	 		+ "WHERE koushin_time = ? AND midashi_id = ?";
			return connection.update(sql,
				dto.midashiName, dto.hyoujiJun, koushinUserId
				,dto.koushinTime, dto.midashiId);
    }

	/**
	* 見出し登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto 見出し
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int upsert(
		Midashi dto
		,String koushinUserId
		 ){
		final String sql =
				"INSERT INTO midashi "
			+ "( midashi_name, hyouji_jun, touroku_user_id, touroku_time, koushin_user_id, koushin_time) "
			+ "VALUES(?, ?, ?, current_timestamp, ?, current_timestamp "
			+ ") ON CONFLICT ON CONSTRAINT midashi_pkey "
			+ "DO UPDATE SET midashi_name = ?, hyouji_jun = ?, koushin_user_id = ?, koushin_time = current_timestamp "
			+ "";
			return connection.update(sql,
				dto.midashiName, dto.hyoujiJun, koushinUserId, koushinUserId
				, dto.midashiName, dto.hyoujiJun, koushinUserId
				);
    }
	
	/**
	 * 見出しから主キー指定でレコードを削除
	 * @param midashiId 見出しID
	 * @return 削除件数
	 */
	public int delete(long midashiId){
		final String sql = "DELETE FROM midashi WHERE midashi_id = ?";
		return connection.update(sql, midashiId);
	}
}
