package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Uf1Ichiran;

/**
 * ユニバーサルフィールド１一覧に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Uf1IchiranAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Uf1Ichiran mapToDto(GMap map){
		return map == null ? null : new Uf1Ichiran(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Uf1Ichiran> mapToDto(List<GMap> mapList){
		List<Uf1Ichiran> dtoList = new ArrayList<Uf1Ichiran>();
		for (var map : mapList) {
			dtoList.add(new Uf1Ichiran(map));
		}
		return dtoList;
	}
	
	/**
	 * ユニバーサルフィールド１一覧のレコード有無を判定
	 * @param uf1Cd UF1コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String uf1Cd) {
		return this.find(uf1Cd) == null ? false : true;
	}
	
	/**
	 * ユニバーサルフィールド１一覧から主キー指定でレコードを取得
	 * @param uf1Cd UF1コード
	 * @return ユニバーサルフィールド１一覧DTO
	 */
	public Uf1Ichiran find(String uf1Cd) {
		final String sql = "SELECT * FROM uf1_ichiran WHERE uf1_cd = ?";
		return mapToDto(connection.find(sql, uf1Cd));
	}
	
	/**
	 * ユニバーサルフィールド１一覧からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ユニバーサルフィールド１一覧DTO>
	 */
	public List<Uf1Ichiran> load() {
		final String sql = "SELECT * FROM uf1_ichiran ORDER BY uf1_cd";
		return mapToDto(connection.load(sql));
	}

	/**
	* ユニバーサルフィールド１一覧登録
	* @param dto ユニバーサルフィールド１一覧
	* @return 件数
	*/
	public int insert(
		Uf1Ichiran dto
	){
		final String sql =
				"INSERT INTO uf1_ichiran "
			+ "VALUES(?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.uf1Cd, dto.uf1NameRyakushiki, dto.kessankiBangou
					);
	}

	/**
	* ユニバーサルフィールド１一覧登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ユニバーサルフィールド１一覧
	* @return 件数
	*/
	public int upsert(
		Uf1Ichiran dto
		 ){
		final String sql =
				"INSERT INTO uf1_ichiran "
			+ "VALUES(?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT uf1_ichiran_pkey "
			+ "DO UPDATE SET uf1_name_ryakushiki = ?, kessanki_bangou = ? "
			+ "";
			return connection.update(sql,
				dto.uf1Cd, dto.uf1NameRyakushiki, dto.kessankiBangou
				, dto.uf1NameRyakushiki, dto.kessankiBangou
				);
    }
	
	/**
	 * ユニバーサルフィールド１一覧から主キー指定でレコードを削除
	 * @param uf1Cd UF1コード
	 * @return 削除件数
	 */
	public int delete(String uf1Cd){
		final String sql = "DELETE FROM uf1_ichiran WHERE uf1_cd = ?";
		return connection.update(sql, uf1Cd);
	}
}
