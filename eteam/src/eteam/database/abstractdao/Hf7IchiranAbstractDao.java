package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Hf7Ichiran;

/**
 * ヘッダフィールド７一覧に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Hf7IchiranAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Hf7Ichiran mapToDto(GMap map){
		return map == null ? null : new Hf7Ichiran(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Hf7Ichiran> mapToDto(List<GMap> mapList){
		List<Hf7Ichiran> dtoList = new ArrayList<Hf7Ichiran>();
		for (var map : mapList) {
			dtoList.add(new Hf7Ichiran(map));
		}
		return dtoList;
	}
	
	/**
	 * ヘッダフィールド７一覧のレコード有無を判定
	 * @param hf7Cd HF7コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String hf7Cd) {
		return this.find(hf7Cd) == null ? false : true;
	}
	
	/**
	 * ヘッダフィールド７一覧から主キー指定でレコードを取得
	 * @param hf7Cd HF7コード
	 * @return ヘッダフィールド７一覧DTO
	 */
	public Hf7Ichiran find(String hf7Cd) {
		final String sql = "SELECT * FROM hf7_ichiran WHERE hf7_cd = ?";
		return mapToDto(connection.find(sql, hf7Cd));
	}
	
	/**
	 * ヘッダフィールド７一覧からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ヘッダフィールド７一覧DTO>
	 */
	public List<Hf7Ichiran> load() {
		final String sql = "SELECT * FROM hf7_ichiran ORDER BY hf7_cd";
		return mapToDto(connection.load(sql));
	}

	/**
	* ヘッダフィールド７一覧登録
	* @param dto ヘッダフィールド７一覧
	* @return 件数
	*/
	public int insert(
		Hf7Ichiran dto
	){
		final String sql =
				"INSERT INTO hf7_ichiran "
			+ "VALUES(?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.hf7Cd, dto.hf7NameRyakushiki, dto.kessankiBangou
					);
	}

	/**
	* ヘッダフィールド７一覧登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ヘッダフィールド７一覧
	* @return 件数
	*/
	public int upsert(
		Hf7Ichiran dto
		 ){
		final String sql =
				"INSERT INTO hf7_ichiran "
			+ "VALUES(?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT hf7_ichiran_pkey "
			+ "DO UPDATE SET hf7_name_ryakushiki = ?, kessanki_bangou = ? "
			+ "";
			return connection.update(sql,
				dto.hf7Cd, dto.hf7NameRyakushiki, dto.kessankiBangou
				, dto.hf7NameRyakushiki, dto.kessankiBangou
				);
    }
	
	/**
	 * ヘッダフィールド７一覧から主キー指定でレコードを削除
	 * @param hf7Cd HF7コード
	 * @return 削除件数
	 */
	public int delete(String hf7Cd){
		final String sql = "DELETE FROM hf7_ichiran WHERE hf7_cd = ?";
		return connection.update(sql, hf7Cd);
	}
}
