package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.JishouDpkbnKanren;

/**
 * 事象伝票区分関連に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class JishouDpkbnKanrenAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected JishouDpkbnKanren mapToDto(GMap map){
		return map == null ? null : new JishouDpkbnKanren(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<JishouDpkbnKanren> mapToDto(List<GMap> mapList){
		List<JishouDpkbnKanren> dtoList = new ArrayList<JishouDpkbnKanren>();
		for (var map : mapList) {
			dtoList.add(new JishouDpkbnKanren(map));
		}
		return dtoList;
	}
	
	/**
	 * 事象伝票区分関連のレコード有無を判定
	 * @param jishouId 事象ID
	 * @param denpyouKbn 伝票区分
	 * @return true:exist false:not exist
	 */
	public boolean exists(long jishouId, String denpyouKbn) {
		return this.find(jishouId, denpyouKbn) == null ? false : true;
	}
	
	/**
	 * 事象伝票区分関連から主キー指定でレコードを取得
	 * @param jishouId 事象ID
	 * @param denpyouKbn 伝票区分
	 * @return 事象伝票区分関連DTO
	 */
	public JishouDpkbnKanren find(long jishouId, String denpyouKbn) {
		final String sql = "SELECT * FROM jishou_dpkbn_kanren WHERE jishou_id = ? AND denpyou_kbn = ?";
		return mapToDto(connection.find(sql, jishouId, denpyouKbn));
	}
	
	/**
	 * 事象伝票区分関連からレコードを全件取得 ※大量データ取得に注意
	 * @return List<事象伝票区分関連DTO>
	 */
	public List<JishouDpkbnKanren> load() {
		final String sql = "SELECT * FROM jishou_dpkbn_kanren ORDER BY jishou_id, denpyou_kbn";
		return mapToDto(connection.load(sql));
	}
	
	/**
	 * 事象伝票区分関連から一部キー指定でレコードを取得
	 * @param jishouId 事象ID
	 * @return List<事象伝票区分関連>DTO
	 */
	public List<JishouDpkbnKanren> load(long jishouId) {
		final String sql = "SELECT * FROM jishou_dpkbn_kanren WHERE jishou_id = ? "
							+ "ORDER BY jishou_id, denpyou_kbn";
		return mapToDto(connection.load(sql, jishouId));
	}

	/**
	* 事象伝票区分関連登録
	* @param dto 事象伝票区分関連
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int insert(
		JishouDpkbnKanren dto
		,String koushinUserId
	){
		final String sql =
				"INSERT INTO jishou_dpkbn_kanren "
			+ "VALUES(?, ?, ?, ?, current_timestamp, ?, current_timestamp "
			+ ")";
			return connection.update(sql,
					dto.jishouId, dto.denpyouKbn, dto.hyoujiJun, koushinUserId, koushinUserId
					);
	}

	/**
	* 事象伝票区分関連の非キー全てと共通列を更新
	* 値設定漏れによるデータ消失防止のためDaoで取得したJishouDpkbnKanrenの使用を前提
	* @param dto 事象伝票区分関連
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int update(
		JishouDpkbnKanren dto
		,String koushinUserId
		 ){
		final String sql =
				"UPDATE jishou_dpkbn_kanren "
		    + "SET hyouji_jun = ?, koushin_user_id = ?, koushin_time = current_timestamp "
	 		+ "WHERE koushin_time = ? AND jishou_id = ? AND denpyou_kbn = ?";
			return connection.update(sql,
				dto.hyoujiJun, koushinUserId
				,dto.koushinTime, dto.jishouId, dto.denpyouKbn);
    }

	/**
	* 事象伝票区分関連登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto 事象伝票区分関連
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int upsert(
		JishouDpkbnKanren dto
		,String koushinUserId
		 ){
		final String sql =
				"INSERT INTO jishou_dpkbn_kanren "
			+ "VALUES(?, ?, ?, ?, current_timestamp, ?, current_timestamp "
			+ ") ON CONFLICT ON CONSTRAINT jishou_dpkbn_kanren_pkey "
			+ "DO UPDATE SET hyouji_jun = ?, koushin_user_id = ?, koushin_time = current_timestamp "
			+ "";
			return connection.update(sql,
				dto.jishouId, dto.denpyouKbn, dto.hyoujiJun, koushinUserId, koushinUserId
				, dto.hyoujiJun, koushinUserId
				);
    }
	
	/**
	 * 事象伝票区分関連から主キー指定でレコードを削除
	 * @param jishouId 事象ID
	 * @param denpyouKbn 伝票区分
	 * @return 削除件数
	 */
	public int delete(long jishouId, String denpyouKbn){
		final String sql = "DELETE FROM jishou_dpkbn_kanren WHERE jishou_id = ? AND denpyou_kbn = ?";
		return connection.update(sql, jishouId, denpyouKbn);
	}
	
	/**
	 * 事象伝票区分関連から一部キー指定でレコードを削除
	 * @param jishouId 事象ID
	 * @return 削除件数
	 */
	public int delete(long jishouId) {
		final String sql = "DELETE FROM jishou_dpkbn_kanren WHERE jishou_id = ? ";
		return connection.update(sql, jishouId);
	}
}
