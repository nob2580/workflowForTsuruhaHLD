package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.BatchHaitaSeigyo;

/**
 * バッチ排他制御に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class BatchHaitaSeigyoAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected BatchHaitaSeigyo mapToDto(GMap map){
		return map == null ? null : new BatchHaitaSeigyo(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<BatchHaitaSeigyo> mapToDto(List<GMap> mapList){
		List<BatchHaitaSeigyo> dtoList = new ArrayList<BatchHaitaSeigyo>();
		for (var map : mapList) {
			dtoList.add(new BatchHaitaSeigyo(map));
		}
		return dtoList;
	}
	
	/**
	 * バッチ排他制御のレコード有無を判定
	 * @param dummy ダミー
	 * @return true:exist false:not exist
	 */
	public boolean exists(String dummy) {
		return this.find(dummy) == null ? false : true;
	}
	
	/**
	 * バッチ排他制御から主キー指定でレコードを取得
	 * @param dummy ダミー
	 * @return バッチ排他制御DTO
	 */
	public BatchHaitaSeigyo find(String dummy) {
		final String sql = "SELECT * FROM batch_haita_seigyo WHERE dummy = ?";
		return mapToDto(connection.find(sql, dummy));
	}
	
	/**
	 * バッチ排他制御からレコードを全件取得 ※大量データ取得に注意
	 * @return List<バッチ排他制御DTO>
	 */
	public List<BatchHaitaSeigyo> load() {
		final String sql = "SELECT * FROM batch_haita_seigyo ORDER BY dummy";
		return mapToDto(connection.load(sql));
	}

	/**
	* バッチ排他制御登録
	* @param dto バッチ排他制御
	* @return 件数
	*/
	public int insert(
		BatchHaitaSeigyo dto
	){
		final String sql =
				"INSERT INTO batch_haita_seigyo "
			+ "VALUES(? "
			+ ")";
			return connection.update(sql,
					dto.dummy
					);
	}
	
	/**
	 * バッチ排他制御から主キー指定でレコードを削除
	 * @param dummy ダミー
	 * @return 削除件数
	 */
	public int delete(String dummy){
		final String sql = "DELETE FROM batch_haita_seigyo WHERE dummy = ?";
		return connection.update(sql, dummy);
	}
}
