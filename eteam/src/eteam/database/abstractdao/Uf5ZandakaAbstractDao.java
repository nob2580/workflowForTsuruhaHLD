package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Uf5Zandaka;

/**
 * ユニバーサルフィールド５残高に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Uf5ZandakaAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Uf5Zandaka mapToDto(GMap map){
		return map == null ? null : new Uf5Zandaka(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Uf5Zandaka> mapToDto(List<GMap> mapList){
		List<Uf5Zandaka> dtoList = new ArrayList<Uf5Zandaka>();
		for (var map : mapList) {
			dtoList.add(new Uf5Zandaka(map));
		}
		return dtoList;
	}
	
	/**
	 * ユニバーサルフィールド５残高のレコード有無を判定
	 * @param uf5Cd UF5コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String uf5Cd, String kamokuGaibuCd) {
		return this.find(uf5Cd, kamokuGaibuCd) == null ? false : true;
	}
	
	/**
	 * ユニバーサルフィールド５残高から主キー指定でレコードを取得
	 * @param uf5Cd UF5コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return ユニバーサルフィールド５残高DTO
	 */
	public Uf5Zandaka find(String uf5Cd, String kamokuGaibuCd) {
		final String sql = "SELECT * FROM uf5_zandaka WHERE uf5_cd = ? AND kamoku_gaibu_cd = ?";
		return mapToDto(connection.find(sql, uf5Cd, kamokuGaibuCd));
	}
	
	/**
	 * ユニバーサルフィールド５残高からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ユニバーサルフィールド５残高DTO>
	 */
	public List<Uf5Zandaka> load() {
		final String sql = "SELECT * FROM uf5_zandaka ORDER BY uf5_cd, kamoku_gaibu_cd";
		return mapToDto(connection.load(sql));
	}
	
	/**
	 * ユニバーサルフィールド５残高から一部キー指定でレコードを取得
	 * @param uf5Cd UF5コード
	 * @return List<ユニバーサルフィールド５残高>DTO
	 */
	public List<Uf5Zandaka> load(String uf5Cd) {
		final String sql = "SELECT * FROM uf5_zandaka WHERE uf5_cd = ? "
							+ "ORDER BY uf5_cd, kamoku_gaibu_cd";
		return mapToDto(connection.load(sql, uf5Cd));
	}

	/**
	* ユニバーサルフィールド５残高登録
	* @param dto ユニバーサルフィールド５残高
	* @return 件数
	*/
	public int insert(
		Uf5Zandaka dto
	){
		final String sql =
				"INSERT INTO uf5_zandaka "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.uf5Cd, dto.kamokuGaibuCd, dto.kessankiBangou, dto.kamokuNaibuCd, dto.uf5NameRyakushiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
					);
	}

	/**
	* ユニバーサルフィールド５残高登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ユニバーサルフィールド５残高
	* @return 件数
	*/
	public int upsert(
		Uf5Zandaka dto
		 ){
		final String sql =
				"INSERT INTO uf5_zandaka "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT uf5_zandaka_pkey "
			+ "DO UPDATE SET kessanki_bangou = ?, kamoku_naibu_cd = ?, uf5_name_ryakushiki = ?, chouhyou_shaturyoku_no = ?, kamoku_name_ryakushiki = ?, kamoku_name_seishiki = ?, taishaku_zokusei = ?, kishu_zandaka = ? "
			+ "";
			return connection.update(sql,
				dto.uf5Cd, dto.kamokuGaibuCd, dto.kessankiBangou, dto.kamokuNaibuCd, dto.uf5NameRyakushiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
				, dto.kessankiBangou, dto.kamokuNaibuCd, dto.uf5NameRyakushiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
				);
    }
	
	/**
	 * ユニバーサルフィールド５残高から主キー指定でレコードを削除
	 * @param uf5Cd UF5コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return 削除件数
	 */
	public int delete(String uf5Cd, String kamokuGaibuCd){
		final String sql = "DELETE FROM uf5_zandaka WHERE uf5_cd = ? AND kamoku_gaibu_cd = ?";
		return connection.update(sql, uf5Cd, kamokuGaibuCd);
	}
	
	/**
	 * ユニバーサルフィールド５残高から一部キー指定でレコードを削除
	 * @param uf5Cd UF5コード
	 * @return 削除件数
	 */
	public int delete(String uf5Cd) {
		final String sql = "DELETE FROM uf5_zandaka WHERE uf5_cd = ? ";
		return connection.update(sql, uf5Cd);
	}
}
