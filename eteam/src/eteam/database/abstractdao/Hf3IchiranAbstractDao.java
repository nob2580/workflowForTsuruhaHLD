package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Hf3Ichiran;

/**
 * ヘッダフィールド３一覧に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Hf3IchiranAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Hf3Ichiran mapToDto(GMap map){
		return map == null ? null : new Hf3Ichiran(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Hf3Ichiran> mapToDto(List<GMap> mapList){
		List<Hf3Ichiran> dtoList = new ArrayList<Hf3Ichiran>();
		for (var map : mapList) {
			dtoList.add(new Hf3Ichiran(map));
		}
		return dtoList;
	}
	
	/**
	 * ヘッダフィールド３一覧のレコード有無を判定
	 * @param hf3Cd HF3コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String hf3Cd) {
		return this.find(hf3Cd) == null ? false : true;
	}
	
	/**
	 * ヘッダフィールド３一覧から主キー指定でレコードを取得
	 * @param hf3Cd HF3コード
	 * @return ヘッダフィールド３一覧DTO
	 */
	public Hf3Ichiran find(String hf3Cd) {
		final String sql = "SELECT * FROM hf3_ichiran WHERE hf3_cd = ?";
		return mapToDto(connection.find(sql, hf3Cd));
	}
	
	/**
	 * ヘッダフィールド３一覧からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ヘッダフィールド３一覧DTO>
	 */
	public List<Hf3Ichiran> load() {
		final String sql = "SELECT * FROM hf3_ichiran ORDER BY hf3_cd";
		return mapToDto(connection.load(sql));
	}

	/**
	* ヘッダフィールド３一覧登録
	* @param dto ヘッダフィールド３一覧
	* @return 件数
	*/
	public int insert(
		Hf3Ichiran dto
	){
		final String sql =
				"INSERT INTO hf3_ichiran "
			+ "VALUES(?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.hf3Cd, dto.hf3NameRyakushiki, dto.kessankiBangou
					);
	}

	/**
	* ヘッダフィールド３一覧登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ヘッダフィールド３一覧
	* @return 件数
	*/
	public int upsert(
		Hf3Ichiran dto
		 ){
		final String sql =
				"INSERT INTO hf3_ichiran "
			+ "VALUES(?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT hf3_ichiran_pkey "
			+ "DO UPDATE SET hf3_name_ryakushiki = ?, kessanki_bangou = ? "
			+ "";
			return connection.update(sql,
				dto.hf3Cd, dto.hf3NameRyakushiki, dto.kessankiBangou
				, dto.hf3NameRyakushiki, dto.kessankiBangou
				);
    }
	
	/**
	 * ヘッダフィールド３一覧から主キー指定でレコードを削除
	 * @param hf3Cd HF3コード
	 * @return 削除件数
	 */
	public int delete(String hf3Cd){
		final String sql = "DELETE FROM hf3_ichiran WHERE hf3_cd = ?";
		return connection.update(sql, hf3Cd);
	}
}
