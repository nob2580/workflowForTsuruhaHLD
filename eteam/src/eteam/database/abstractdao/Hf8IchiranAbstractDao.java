package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Hf8Ichiran;

/**
 * ヘッダフィールド８一覧に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Hf8IchiranAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Hf8Ichiran mapToDto(GMap map){
		return map == null ? null : new Hf8Ichiran(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Hf8Ichiran> mapToDto(List<GMap> mapList){
		List<Hf8Ichiran> dtoList = new ArrayList<Hf8Ichiran>();
		for (var map : mapList) {
			dtoList.add(new Hf8Ichiran(map));
		}
		return dtoList;
	}
	
	/**
	 * ヘッダフィールド８一覧のレコード有無を判定
	 * @param hf8Cd HF8コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String hf8Cd) {
		return this.find(hf8Cd) == null ? false : true;
	}
	
	/**
	 * ヘッダフィールド８一覧から主キー指定でレコードを取得
	 * @param hf8Cd HF8コード
	 * @return ヘッダフィールド８一覧DTO
	 */
	public Hf8Ichiran find(String hf8Cd) {
		final String sql = "SELECT * FROM hf8_ichiran WHERE hf8_cd = ?";
		return mapToDto(connection.find(sql, hf8Cd));
	}
	
	/**
	 * ヘッダフィールド８一覧からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ヘッダフィールド８一覧DTO>
	 */
	public List<Hf8Ichiran> load() {
		final String sql = "SELECT * FROM hf8_ichiran ORDER BY hf8_cd";
		return mapToDto(connection.load(sql));
	}

	/**
	* ヘッダフィールド８一覧登録
	* @param dto ヘッダフィールド８一覧
	* @return 件数
	*/
	public int insert(
		Hf8Ichiran dto
	){
		final String sql =
				"INSERT INTO hf8_ichiran "
			+ "VALUES(?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.hf8Cd, dto.hf8NameRyakushiki, dto.kessankiBangou
					);
	}

	/**
	* ヘッダフィールド８一覧登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ヘッダフィールド８一覧
	* @return 件数
	*/
	public int upsert(
		Hf8Ichiran dto
		 ){
		final String sql =
				"INSERT INTO hf8_ichiran "
			+ "VALUES(?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT hf8_ichiran_pkey "
			+ "DO UPDATE SET hf8_name_ryakushiki = ?, kessanki_bangou = ? "
			+ "";
			return connection.update(sql,
				dto.hf8Cd, dto.hf8NameRyakushiki, dto.kessankiBangou
				, dto.hf8NameRyakushiki, dto.kessankiBangou
				);
    }
	
	/**
	 * ヘッダフィールド８一覧から主キー指定でレコードを削除
	 * @param hf8Cd HF8コード
	 * @return 削除件数
	 */
	public int delete(String hf8Cd){
		final String sql = "DELETE FROM hf8_ichiran WHERE hf8_cd = ?";
		return connection.update(sql, hf8Cd);
	}
}
