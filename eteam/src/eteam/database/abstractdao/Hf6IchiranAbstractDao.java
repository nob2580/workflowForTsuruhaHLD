package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Hf6Ichiran;

/**
 * ヘッダフィールド６一覧に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Hf6IchiranAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Hf6Ichiran mapToDto(GMap map){
		return map == null ? null : new Hf6Ichiran(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Hf6Ichiran> mapToDto(List<GMap> mapList){
		List<Hf6Ichiran> dtoList = new ArrayList<Hf6Ichiran>();
		for (var map : mapList) {
			dtoList.add(new Hf6Ichiran(map));
		}
		return dtoList;
	}
	
	/**
	 * ヘッダフィールド６一覧のレコード有無を判定
	 * @param hf6Cd HF6コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String hf6Cd) {
		return this.find(hf6Cd) == null ? false : true;
	}
	
	/**
	 * ヘッダフィールド６一覧から主キー指定でレコードを取得
	 * @param hf6Cd HF6コード
	 * @return ヘッダフィールド６一覧DTO
	 */
	public Hf6Ichiran find(String hf6Cd) {
		final String sql = "SELECT * FROM hf6_ichiran WHERE hf6_cd = ?";
		return mapToDto(connection.find(sql, hf6Cd));
	}
	
	/**
	 * ヘッダフィールド６一覧からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ヘッダフィールド６一覧DTO>
	 */
	public List<Hf6Ichiran> load() {
		final String sql = "SELECT * FROM hf6_ichiran ORDER BY hf6_cd";
		return mapToDto(connection.load(sql));
	}

	/**
	* ヘッダフィールド６一覧登録
	* @param dto ヘッダフィールド６一覧
	* @return 件数
	*/
	public int insert(
		Hf6Ichiran dto
	){
		final String sql =
				"INSERT INTO hf6_ichiran "
			+ "VALUES(?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.hf6Cd, dto.hf6NameRyakushiki, dto.kessankiBangou
					);
	}

	/**
	* ヘッダフィールド６一覧登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ヘッダフィールド６一覧
	* @return 件数
	*/
	public int upsert(
		Hf6Ichiran dto
		 ){
		final String sql =
				"INSERT INTO hf6_ichiran "
			+ "VALUES(?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT hf6_ichiran_pkey "
			+ "DO UPDATE SET hf6_name_ryakushiki = ?, kessanki_bangou = ? "
			+ "";
			return connection.update(sql,
				dto.hf6Cd, dto.hf6NameRyakushiki, dto.kessankiBangou
				, dto.hf6NameRyakushiki, dto.kessankiBangou
				);
    }
	
	/**
	 * ヘッダフィールド６一覧から主キー指定でレコードを削除
	 * @param hf6Cd HF6コード
	 * @return 削除件数
	 */
	public int delete(String hf6Cd){
		final String sql = "DELETE FROM hf6_ichiran WHERE hf6_cd = ?";
		return connection.update(sql, hf6Cd);
	}
}
