package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.InformationSort;

/**
 * インフォメーション順序に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class InformationSortAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected InformationSort mapToDto(GMap map){
		return map == null ? null : new InformationSort(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<InformationSort> mapToDto(List<GMap> mapList){
		List<InformationSort> dtoList = new ArrayList<InformationSort>();
		for (var map : mapList) {
			dtoList.add(new InformationSort(map));
		}
		return dtoList;
	}
	
	/**
	 * インフォメーション順序のレコード有無を判定
	 * @param infoId インフォメーションID
	 * @return true:exist false:not exist
	 */
	public boolean exists(String infoId) {
		return this.find(infoId) == null ? false : true;
	}
	
	/**
	 * インフォメーション順序から主キー指定でレコードを取得
	 * @param infoId インフォメーションID
	 * @return インフォメーション順序DTO
	 */
	public InformationSort find(String infoId) {
		final String sql = "SELECT * FROM information_sort WHERE info_id = ?";
		return mapToDto(connection.find(sql, infoId));
	}
	
	/**
	 * インフォメーション順序からレコードを全件取得 ※大量データ取得に注意
	 * @return List<インフォメーション順序DTO>
	 */
	public List<InformationSort> load() {
		final String sql = "SELECT * FROM information_sort ORDER BY info_id";
		return mapToDto(connection.load(sql));
	}

	/**
	* インフォメーション順序登録
	* @param dto インフォメーション順序
	* @return 件数
	*/
	public int insert(
		InformationSort dto
	){
		final String sql =
				"INSERT INTO information_sort "
			+ "VALUES(?, ? "
			+ ")";
			return connection.update(sql,
					dto.infoId, dto.hyoujiJun
					);
	}

	/**
	* インフォメーション順序登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto インフォメーション順序
	* @return 件数
	*/
	public int upsert(
		InformationSort dto
		 ){
		final String sql =
				"INSERT INTO information_sort "
			+ "VALUES(?, ? "
			+ ") ON CONFLICT ON CONSTRAINT information_sort_pkey "
			+ "DO UPDATE SET hyouji_jun = ? "
			+ "";
			return connection.update(sql,
				dto.infoId, dto.hyoujiJun
				, dto.hyoujiJun
				);
    }
	
	/**
	 * インフォメーション順序から主キー指定でレコードを削除
	 * @param infoId インフォメーションID
	 * @return 削除件数
	 */
	public int delete(String infoId){
		final String sql = "DELETE FROM information_sort WHERE info_id = ?";
		return connection.update(sql, infoId);
	}
}
