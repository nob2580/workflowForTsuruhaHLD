package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Uf2Ichiran;

/**
 * ユニバーサルフィールド２一覧に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Uf2IchiranAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Uf2Ichiran mapToDto(GMap map){
		return map == null ? null : new Uf2Ichiran(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Uf2Ichiran> mapToDto(List<GMap> mapList){
		List<Uf2Ichiran> dtoList = new ArrayList<Uf2Ichiran>();
		for (var map : mapList) {
			dtoList.add(new Uf2Ichiran(map));
		}
		return dtoList;
	}
	
	/**
	 * ユニバーサルフィールド２一覧のレコード有無を判定
	 * @param uf2Cd UF2コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String uf2Cd) {
		return this.find(uf2Cd) == null ? false : true;
	}
	
	/**
	 * ユニバーサルフィールド２一覧から主キー指定でレコードを取得
	 * @param uf2Cd UF2コード
	 * @return ユニバーサルフィールド２一覧DTO
	 */
	public Uf2Ichiran find(String uf2Cd) {
		final String sql = "SELECT * FROM uf2_ichiran WHERE uf2_cd = ?";
		return mapToDto(connection.find(sql, uf2Cd));
	}
	
	/**
	 * ユニバーサルフィールド２一覧からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ユニバーサルフィールド２一覧DTO>
	 */
	public List<Uf2Ichiran> load() {
		final String sql = "SELECT * FROM uf2_ichiran ORDER BY uf2_cd";
		return mapToDto(connection.load(sql));
	}

	/**
	* ユニバーサルフィールド２一覧登録
	* @param dto ユニバーサルフィールド２一覧
	* @return 件数
	*/
	public int insert(
		Uf2Ichiran dto
	){
		final String sql =
				"INSERT INTO uf2_ichiran "
			+ "VALUES(?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.uf2Cd, dto.uf2NameRyakushiki, dto.kessankiBangou
					);
	}

	/**
	* ユニバーサルフィールド２一覧登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ユニバーサルフィールド２一覧
	* @return 件数
	*/
	public int upsert(
		Uf2Ichiran dto
		 ){
		final String sql =
				"INSERT INTO uf2_ichiran "
			+ "VALUES(?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT uf2_ichiran_pkey "
			+ "DO UPDATE SET uf2_name_ryakushiki = ?, kessanki_bangou = ? "
			+ "";
			return connection.update(sql,
				dto.uf2Cd, dto.uf2NameRyakushiki, dto.kessankiBangou
				, dto.uf2NameRyakushiki, dto.kessankiBangou
				);
    }
	
	/**
	 * ユニバーサルフィールド２一覧から主キー指定でレコードを削除
	 * @param uf2Cd UF2コード
	 * @return 削除件数
	 */
	public int delete(String uf2Cd){
		final String sql = "DELETE FROM uf2_ichiran WHERE uf2_cd = ?";
		return connection.update(sql, uf2Cd);
	}
}
