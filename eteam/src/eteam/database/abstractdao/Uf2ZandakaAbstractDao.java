package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Uf2Zandaka;

/**
 * ユニバーサルフィールド２残高に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Uf2ZandakaAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Uf2Zandaka mapToDto(GMap map){
		return map == null ? null : new Uf2Zandaka(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Uf2Zandaka> mapToDto(List<GMap> mapList){
		List<Uf2Zandaka> dtoList = new ArrayList<Uf2Zandaka>();
		for (var map : mapList) {
			dtoList.add(new Uf2Zandaka(map));
		}
		return dtoList;
	}
	
	/**
	 * ユニバーサルフィールド２残高のレコード有無を判定
	 * @param uf2Cd UF2コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String uf2Cd, String kamokuGaibuCd) {
		return this.find(uf2Cd, kamokuGaibuCd) == null ? false : true;
	}
	
	/**
	 * ユニバーサルフィールド２残高から主キー指定でレコードを取得
	 * @param uf2Cd UF2コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return ユニバーサルフィールド２残高DTO
	 */
	public Uf2Zandaka find(String uf2Cd, String kamokuGaibuCd) {
		final String sql = "SELECT * FROM uf2_zandaka WHERE uf2_cd = ? AND kamoku_gaibu_cd = ?";
		return mapToDto(connection.find(sql, uf2Cd, kamokuGaibuCd));
	}
	
	/**
	 * ユニバーサルフィールド２残高からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ユニバーサルフィールド２残高DTO>
	 */
	public List<Uf2Zandaka> load() {
		final String sql = "SELECT * FROM uf2_zandaka ORDER BY uf2_cd, kamoku_gaibu_cd";
		return mapToDto(connection.load(sql));
	}
	
	/**
	 * ユニバーサルフィールド２残高から一部キー指定でレコードを取得
	 * @param uf2Cd UF2コード
	 * @return List<ユニバーサルフィールド２残高>DTO
	 */
	public List<Uf2Zandaka> load(String uf2Cd) {
		final String sql = "SELECT * FROM uf2_zandaka WHERE uf2_cd = ? "
							+ "ORDER BY uf2_cd, kamoku_gaibu_cd";
		return mapToDto(connection.load(sql, uf2Cd));
	}

	/**
	* ユニバーサルフィールド２残高登録
	* @param dto ユニバーサルフィールド２残高
	* @return 件数
	*/
	public int insert(
		Uf2Zandaka dto
	){
		final String sql =
				"INSERT INTO uf2_zandaka "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.uf2Cd, dto.kamokuGaibuCd, dto.kessankiBangou, dto.kamokuNaibuCd, dto.uf2NameRyakushiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
					);
	}

	/**
	* ユニバーサルフィールド２残高登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ユニバーサルフィールド２残高
	* @return 件数
	*/
	public int upsert(
		Uf2Zandaka dto
		 ){
		final String sql =
				"INSERT INTO uf2_zandaka "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT uf2_zandaka_pkey "
			+ "DO UPDATE SET kessanki_bangou = ?, kamoku_naibu_cd = ?, uf2_name_ryakushiki = ?, chouhyou_shaturyoku_no = ?, kamoku_name_ryakushiki = ?, kamoku_name_seishiki = ?, taishaku_zokusei = ?, kishu_zandaka = ? "
			+ "";
			return connection.update(sql,
				dto.uf2Cd, dto.kamokuGaibuCd, dto.kessankiBangou, dto.kamokuNaibuCd, dto.uf2NameRyakushiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
				, dto.kessankiBangou, dto.kamokuNaibuCd, dto.uf2NameRyakushiki, dto.chouhyouShaturyokuNo, dto.kamokuNameRyakushiki, dto.kamokuNameSeishiki, dto.taishakuZokusei, dto.kishuZandaka
				);
    }
	
	/**
	 * ユニバーサルフィールド２残高から主キー指定でレコードを削除
	 * @param uf2Cd UF2コード
	 * @param kamokuGaibuCd 科目外部コード
	 * @return 削除件数
	 */
	public int delete(String uf2Cd, String kamokuGaibuCd){
		final String sql = "DELETE FROM uf2_zandaka WHERE uf2_cd = ? AND kamoku_gaibu_cd = ?";
		return connection.update(sql, uf2Cd, kamokuGaibuCd);
	}
	
	/**
	 * ユニバーサルフィールド２残高から一部キー指定でレコードを削除
	 * @param uf2Cd UF2コード
	 * @return 削除件数
	 */
	public int delete(String uf2Cd) {
		final String sql = "DELETE FROM uf2_zandaka WHERE uf2_cd = ? ";
		return connection.update(sql, uf2Cd);
	}
}
