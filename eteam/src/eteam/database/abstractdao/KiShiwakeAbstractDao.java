package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.KiShiwake;

/**
 * （期別）財務仕訳に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class KiShiwakeAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected KiShiwake mapToDto(GMap map){
		return map == null ? null : new KiShiwake(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<KiShiwake> mapToDto(List<GMap> mapList){
		List<KiShiwake> dtoList = new ArrayList<KiShiwake>();
		for (var map : mapList) {
			dtoList.add(new KiShiwake(map));
		}
		return dtoList;
	}
	
	/**
	 * （期別）財務仕訳のレコード有無を判定
	 * @param kesn 内部決算期
	 * @param dkei 経過月
	 * @param dseq 伝票SEQ
	 * @param sseq 仕訳SEQ
	 * @return true:exist false:not exist
	 */
	public boolean exists(int kesn, int dkei, int dseq, int sseq) {
		return this.find(kesn, dkei, dseq, sseq) == null ? false : true;
	}
	
	/**
	 * （期別）財務仕訳から主キー指定でレコードを取得
	 * @param kesn 内部決算期
	 * @param dkei 経過月
	 * @param dseq 伝票SEQ
	 * @param sseq 仕訳SEQ
	 * @return （期別）財務仕訳DTO
	 */
	public KiShiwake find(int kesn, int dkei, int dseq, int sseq) {
		final String sql = "SELECT * FROM ki_shiwake WHERE kesn = ? AND dkei = ? AND dseq = ? AND sseq = ?";
		return mapToDto(connection.find(sql, kesn, dkei, dseq, sseq));
	}
	
	/**
	 * （期別）財務仕訳からレコードを全件取得 ※大量データ取得に注意
	 * @return List<（期別）財務仕訳DTO>
	 */
	public List<KiShiwake> load() {
		final String sql = "SELECT * FROM ki_shiwake ORDER BY kesn, dkei, dseq, sseq";
		return mapToDto(connection.load(sql));
	}
	
	/**
	 * （期別）財務仕訳から一部キー指定でレコードを取得
	 * @param kesn 内部決算期
	 * @return List<（期別）財務仕訳>DTO
	 */
	public List<KiShiwake> load(int kesn) {
		final String sql = "SELECT * FROM ki_shiwake WHERE kesn = ? "
							+ "ORDER BY kesn, dkei, dseq, sseq";
		return mapToDto(connection.load(sql, kesn));
	}
	
	/**
	 * （期別）財務仕訳から一部キー指定でレコードを取得
	 * @param kesn 内部決算期
	 * @param dkei 経過月
	 * @return List<（期別）財務仕訳>DTO
	 */
	public List<KiShiwake> load(int kesn, int dkei) {
		final String sql = "SELECT * FROM ki_shiwake WHERE kesn = ?  AND dkei = ? "
							+ "ORDER BY kesn, dkei, dseq, sseq";
		return mapToDto(connection.load(sql, kesn, dkei));
	}
	
	/**
	 * （期別）財務仕訳から一部キー指定でレコードを取得
	 * @param kesn 内部決算期
	 * @param dkei 経過月
	 * @param dseq 伝票SEQ
	 * @return List<（期別）財務仕訳>DTO
	 */
	public List<KiShiwake> load(int kesn, int dkei, int dseq) {
		final String sql = "SELECT * FROM ki_shiwake WHERE kesn = ?  AND dkei = ?  AND dseq = ? "
							+ "ORDER BY kesn, dkei, dseq, sseq";
		return mapToDto(connection.load(sql, kesn, dkei, dseq));
	}

	/**
	* （期別）財務仕訳登録
	* @param dto （期別）財務仕訳
	* @return 件数
	*/
	public int insert(
		KiShiwake dto
	){
		final String sql =
				"INSERT INTO ki_shiwake "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.kesn, dto.dkei, dto.dseq, dto.sseq, dto.dymd, dto.dcno, dto.valu, dto.rkmk, dto.reda, dto.rbmn, dto.rtky, dto.rtor, dto.skmk, dto.seda, dto.sbmn, dto.stky, dto.stor, dto.fway
					);
	}

	/**
	* （期別）財務仕訳登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto （期別）財務仕訳
	* @return 件数
	*/
	public int upsert(
		KiShiwake dto
		 ){
		final String sql =
				"INSERT INTO ki_shiwake "
			+ "VALUES(?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT ki_shiwake_pkey "
			+ "DO UPDATE SET dymd = ?, dcno = ?, valu = ?, rkmk = ?, reda = ?, rbmn = ?, rtky = ?, rtor = ?, skmk = ?, seda = ?, sbmn = ?, stky = ?, stor = ?, fway = ? "
			+ "";
			return connection.update(sql,
				dto.kesn, dto.dkei, dto.dseq, dto.sseq, dto.dymd, dto.dcno, dto.valu, dto.rkmk, dto.reda, dto.rbmn, dto.rtky, dto.rtor, dto.skmk, dto.seda, dto.sbmn, dto.stky, dto.stor, dto.fway
				, dto.dymd, dto.dcno, dto.valu, dto.rkmk, dto.reda, dto.rbmn, dto.rtky, dto.rtor, dto.skmk, dto.seda, dto.sbmn, dto.stky, dto.stor, dto.fway
				);
    }
	
	/**
	 * （期別）財務仕訳から主キー指定でレコードを削除
	 * @param kesn 内部決算期
	 * @param dkei 経過月
	 * @param dseq 伝票SEQ
	 * @param sseq 仕訳SEQ
	 * @return 削除件数
	 */
	public int delete(int kesn, int dkei, int dseq, int sseq){
		final String sql = "DELETE FROM ki_shiwake WHERE kesn = ? AND dkei = ? AND dseq = ? AND sseq = ?";
		return connection.update(sql, kesn, dkei, dseq, sseq);
	}
	
	/**
	 * （期別）財務仕訳から一部キー指定でレコードを削除
	 * @param kesn 内部決算期
	 * @return 削除件数
	 */
	public int delete(int kesn) {
		final String sql = "DELETE FROM ki_shiwake WHERE kesn = ? ";
		return connection.update(sql, kesn);
	}
	
	/**
	 * （期別）財務仕訳から一部キー指定でレコードを削除
	 * @param kesn 内部決算期
	 * @param dkei 経過月
	 * @return 削除件数
	 */
	public int delete(int kesn, int dkei) {
		final String sql = "DELETE FROM ki_shiwake WHERE kesn = ?  AND dkei = ? ";
		return connection.update(sql, kesn, dkei);
	}
	
	/**
	 * （期別）財務仕訳から一部キー指定でレコードを削除
	 * @param kesn 内部決算期
	 * @param dkei 経過月
	 * @param dseq 伝票SEQ
	 * @return 削除件数
	 */
	public int delete(int kesn, int dkei, int dseq) {
		final String sql = "DELETE FROM ki_shiwake WHERE kesn = ?  AND dkei = ?  AND dseq = ? ";
		return connection.update(sql, kesn, dkei, dseq);
	}
}
