package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Uf9Ichiran;

/**
 * ユニバーサルフィールド９一覧に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Uf9IchiranAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Uf9Ichiran mapToDto(GMap map){
		return map == null ? null : new Uf9Ichiran(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Uf9Ichiran> mapToDto(List<GMap> mapList){
		List<Uf9Ichiran> dtoList = new ArrayList<Uf9Ichiran>();
		for (var map : mapList) {
			dtoList.add(new Uf9Ichiran(map));
		}
		return dtoList;
	}
	
	/**
	 * ユニバーサルフィールド９一覧のレコード有無を判定
	 * @param uf9Cd UF9コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String uf9Cd) {
		return this.find(uf9Cd) == null ? false : true;
	}
	
	/**
	 * ユニバーサルフィールド９一覧から主キー指定でレコードを取得
	 * @param uf9Cd UF9コード
	 * @return ユニバーサルフィールド９一覧DTO
	 */
	public Uf9Ichiran find(String uf9Cd) {
		final String sql = "SELECT * FROM uf9_ichiran WHERE uf9_cd = ?";
		return mapToDto(connection.find(sql, uf9Cd));
	}
	
	/**
	 * ユニバーサルフィールド９一覧からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ユニバーサルフィールド９一覧DTO>
	 */
	public List<Uf9Ichiran> load() {
		final String sql = "SELECT * FROM uf9_ichiran ORDER BY uf9_cd";
		return mapToDto(connection.load(sql));
	}

	/**
	* ユニバーサルフィールド９一覧登録
	* @param dto ユニバーサルフィールド９一覧
	* @return 件数
	*/
	public int insert(
		Uf9Ichiran dto
	){
		final String sql =
				"INSERT INTO uf9_ichiran "
			+ "VALUES(?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.uf9Cd, dto.uf9NameRyakushiki, dto.kessankiBangou
					);
	}

	/**
	* ユニバーサルフィールド９一覧登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ユニバーサルフィールド９一覧
	* @return 件数
	*/
	public int upsert(
		Uf9Ichiran dto
		 ){
		final String sql =
				"INSERT INTO uf9_ichiran "
			+ "VALUES(?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT uf9_ichiran_pkey "
			+ "DO UPDATE SET uf9_name_ryakushiki = ?, kessanki_bangou = ? "
			+ "";
			return connection.update(sql,
				dto.uf9Cd, dto.uf9NameRyakushiki, dto.kessankiBangou
				, dto.uf9NameRyakushiki, dto.kessankiBangou
				);
    }
	
	/**
	 * ユニバーサルフィールド９一覧から主キー指定でレコードを削除
	 * @param uf9Cd UF9コード
	 * @return 削除件数
	 */
	public int delete(String uf9Cd){
		final String sql = "DELETE FROM uf9_ichiran WHERE uf9_cd = ?";
		return connection.update(sql, uf9Cd);
	}
}
