package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.KaniTodokeVersion;

/**
 * 届出ジェネレータバージョンに対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class KaniTodokeVersionAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected KaniTodokeVersion mapToDto(GMap map){
		return map == null ? null : new KaniTodokeVersion(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<KaniTodokeVersion> mapToDto(List<GMap> mapList){
		List<KaniTodokeVersion> dtoList = new ArrayList<KaniTodokeVersion>();
		for (var map : mapList) {
			dtoList.add(new KaniTodokeVersion(map));
		}
		return dtoList;
	}
	
	/**
	 * 届出ジェネレータバージョンのレコード有無を判定
	 * @param denpyouKbn 伝票区分
	 * @param version バージョン
	 * @return true:exist false:not exist
	 */
	public boolean exists(String denpyouKbn, int version) {
		return this.find(denpyouKbn, version) == null ? false : true;
	}
	
	/**
	 * 届出ジェネレータバージョンから主キー指定でレコードを取得
	 * @param denpyouKbn 伝票区分
	 * @param version バージョン
	 * @return 届出ジェネレータバージョンDTO
	 */
	public KaniTodokeVersion find(String denpyouKbn, int version) {
		final String sql = "SELECT * FROM kani_todoke_version WHERE denpyou_kbn = ? AND version = ?";
		return mapToDto(connection.find(sql, denpyouKbn, version));
	}
	
	/**
	 * 届出ジェネレータバージョンからレコードを全件取得 ※大量データ取得に注意
	 * @return List<届出ジェネレータバージョンDTO>
	 */
	public List<KaniTodokeVersion> load() {
		final String sql = "SELECT * FROM kani_todoke_version ORDER BY denpyou_kbn, version";
		return mapToDto(connection.load(sql));
	}
	
	/**
	 * 届出ジェネレータバージョンから一部キー指定でレコードを取得
	 * @param denpyouKbn 伝票区分
	 * @return List<届出ジェネレータバージョン>DTO
	 */
	public List<KaniTodokeVersion> load(String denpyouKbn) {
		final String sql = "SELECT * FROM kani_todoke_version WHERE denpyou_kbn = ? "
							+ "ORDER BY denpyou_kbn, version";
		return mapToDto(connection.load(sql, denpyouKbn));
	}

	/**
	* 届出ジェネレータバージョン登録
	* @param dto 届出ジェネレータバージョン
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int insert(
		KaniTodokeVersion dto
		,String koushinUserId
	){
		final String sql =
				"INSERT INTO kani_todoke_version "
			+ "VALUES(?, ?, ?, ?, ?, current_timestamp, ?, current_timestamp "
			+ ")";
			return connection.update(sql,
					dto.denpyouKbn, dto.version, dto.denpyouShubetsu, dto.naiyou, koushinUserId, koushinUserId
					);
	}

	/**
	* 届出ジェネレータバージョンの非キー全てと共通列を更新
	* 値設定漏れによるデータ消失防止のためDaoで取得したKaniTodokeVersionの使用を前提
	* @param dto 届出ジェネレータバージョン
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int update(
		KaniTodokeVersion dto
		,String koushinUserId
		 ){
		final String sql =
				"UPDATE kani_todoke_version "
		    + "SET denpyou_shubetsu = ?, naiyou = ?, koushin_user_id = ?, koushin_time = current_timestamp "
	 		+ "WHERE koushin_time = ? AND denpyou_kbn = ? AND version = ?";
			return connection.update(sql,
				dto.denpyouShubetsu, dto.naiyou, koushinUserId
				,dto.koushinTime, dto.denpyouKbn, dto.version);
    }

	/**
	* 届出ジェネレータバージョン登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto 届出ジェネレータバージョン
	* @param koushinUserId 更新ユーザーID
	* @return 件数
	*/
	public int upsert(
		KaniTodokeVersion dto
		,String koushinUserId
		 ){
		final String sql =
				"INSERT INTO kani_todoke_version "
			+ "VALUES(?, ?, ?, ?, ?, current_timestamp, ?, current_timestamp "
			+ ") ON CONFLICT ON CONSTRAINT kani_todoke_version_pkey "
			+ "DO UPDATE SET denpyou_shubetsu = ?, naiyou = ?, koushin_user_id = ?, koushin_time = current_timestamp "
			+ "";
			return connection.update(sql,
				dto.denpyouKbn, dto.version, dto.denpyouShubetsu, dto.naiyou, koushinUserId, koushinUserId
				, dto.denpyouShubetsu, dto.naiyou, koushinUserId
				);
    }
	
	/**
	 * 届出ジェネレータバージョンから主キー指定でレコードを削除
	 * @param denpyouKbn 伝票区分
	 * @param version バージョン
	 * @return 削除件数
	 */
	public int delete(String denpyouKbn, int version){
		final String sql = "DELETE FROM kani_todoke_version WHERE denpyou_kbn = ? AND version = ?";
		return connection.update(sql, denpyouKbn, version);
	}
	
	/**
	 * 届出ジェネレータバージョンから一部キー指定でレコードを削除
	 * @param denpyouKbn 伝票区分
	 * @return 削除件数
	 */
	public int delete(String denpyouKbn) {
		final String sql = "DELETE FROM kani_todoke_version WHERE denpyou_kbn = ? ";
		return connection.update(sql, denpyouKbn);
	}
}
