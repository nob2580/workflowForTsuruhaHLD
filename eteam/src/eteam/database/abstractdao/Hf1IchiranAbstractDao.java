package eteam.database.abstractdao;

import java.util.ArrayList;
import java.util.List;

import eteam.base.EteamAbstractLogic;
import eteam.base.GMap;
import eteam.database.dto.Hf1Ichiran;

/**
 * ヘッダフィールド１一覧に対する標準データ操作クラス
 * This code was generated by the tool.
 * This code can not be changed.
 * @author CodeGenerator
 */
public abstract class Hf1IchiranAbstractDao extends EteamAbstractLogic {

	/**
	* @param map GMap
	* @return dto (レコードが存在しなければNull)
	*/
	protected Hf1Ichiran mapToDto(GMap map){
		return map == null ? null : new Hf1Ichiran(map);
	}

	/**
	* @param mapList 検索結果GMap
	* @return dtoList
	*/
	protected List<Hf1Ichiran> mapToDto(List<GMap> mapList){
		List<Hf1Ichiran> dtoList = new ArrayList<Hf1Ichiran>();
		for (var map : mapList) {
			dtoList.add(new Hf1Ichiran(map));
		}
		return dtoList;
	}
	
	/**
	 * ヘッダフィールド１一覧のレコード有無を判定
	 * @param hf1Cd HF1コード
	 * @return true:exist false:not exist
	 */
	public boolean exists(String hf1Cd) {
		return this.find(hf1Cd) == null ? false : true;
	}
	
	/**
	 * ヘッダフィールド１一覧から主キー指定でレコードを取得
	 * @param hf1Cd HF1コード
	 * @return ヘッダフィールド１一覧DTO
	 */
	public Hf1Ichiran find(String hf1Cd) {
		final String sql = "SELECT * FROM hf1_ichiran WHERE hf1_cd = ?";
		return mapToDto(connection.find(sql, hf1Cd));
	}
	
	/**
	 * ヘッダフィールド１一覧からレコードを全件取得 ※大量データ取得に注意
	 * @return List<ヘッダフィールド１一覧DTO>
	 */
	public List<Hf1Ichiran> load() {
		final String sql = "SELECT * FROM hf1_ichiran ORDER BY hf1_cd";
		return mapToDto(connection.load(sql));
	}

	/**
	* ヘッダフィールド１一覧登録
	* @param dto ヘッダフィールド１一覧
	* @return 件数
	*/
	public int insert(
		Hf1Ichiran dto
	){
		final String sql =
				"INSERT INTO hf1_ichiran "
			+ "VALUES(?, ?, ? "
			+ ")";
			return connection.update(sql,
					dto.hf1Cd, dto.hf1NameRyakushiki, dto.kessankiBangou
					);
	}

	/**
	* ヘッダフィールド１一覧登録or更新
	* 仕様上Insert/Updateの一方のみ処理される場合は使用不可
	* @param dto ヘッダフィールド１一覧
	* @return 件数
	*/
	public int upsert(
		Hf1Ichiran dto
		 ){
		final String sql =
				"INSERT INTO hf1_ichiran "
			+ "VALUES(?, ?, ? "
			+ ") ON CONFLICT ON CONSTRAINT hf1_ichiran_pkey "
			+ "DO UPDATE SET hf1_name_ryakushiki = ?, kessanki_bangou = ? "
			+ "";
			return connection.update(sql,
				dto.hf1Cd, dto.hf1NameRyakushiki, dto.kessankiBangou
				, dto.hf1NameRyakushiki, dto.kessankiBangou
				);
    }
	
	/**
	 * ヘッダフィールド１一覧から主キー指定でレコードを削除
	 * @param hf1Cd HF1コード
	 * @return 削除件数
	 */
	public int delete(String hf1Cd){
		final String sql = "DELETE FROM hf1_ichiran WHERE hf1_cd = ?";
		return connection.update(sql, hf1Cd);
	}
}
